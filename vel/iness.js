if (scales.length > 0) {
    // Place the code to be executed if the condition is true inside the curly braces
    console.log('scales array has elements');
    // Additional code can go here
} else {
    // Place the code to be executed if the condition is false inside the else block
    console.log('scales array is empty');
    // Additional code can go here
}
